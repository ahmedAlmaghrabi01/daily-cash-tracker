{
  "name": "daily-cash-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0",
    "googleapis": "^159.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^23.5.0"
  }
}
